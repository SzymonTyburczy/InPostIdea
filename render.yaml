services:
  - type: web
    name: inpost-locator-pro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: ENV
        value: production
      - key: PYTHON_VERSION
        value: "3.10.0"
    plan: free
    autoDeploy: true
